# VoxelRayBenchmark

Publication bootstrap for VoxelRayBenchmark v0.6.1.

The repository workflow will materialize the complete C++23 source tree from the verified package, commit it to `main`, and publish the tagged GitHub release with the ZIP and SHA-256 checksum.
